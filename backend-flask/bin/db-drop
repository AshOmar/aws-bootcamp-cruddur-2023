#! /usr/bin/bash
BRED='\033[1;31m'
NC='\033[0m' # No Color

echo -e "\n${BRED}=== DB Drop Start ===${NC}\n"

NODBURL=$(sed 's/\/cruddur//g' <<<"$CONNECTION_URL")
psql $NODBURL -c "DROP DATABASE cruddur;"

echo -e "\n${BRED}=== DB Drop Finish ===${NC}\n"